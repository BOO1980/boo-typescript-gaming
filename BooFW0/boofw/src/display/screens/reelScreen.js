import Screen from "../core/screen";

export default class ReelScreen extends Screen {
  //export default class ReelScreen {
  constructor(config) {
    super(config);
    console.log("ReelScreen: constructor");
  }
}
